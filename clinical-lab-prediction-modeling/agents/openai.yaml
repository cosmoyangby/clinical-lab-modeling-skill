interface:
  display_name: "Clinical Lab Prediction Modeling"
  short_description: "BMJ-aligned clinical lab model frameworks"
  default_prompt: "Use $clinical-lab-prediction-modeling to design a BMJ-aligned clinical lab prediction or diagnostic model framework, including intake, sample size feasibility, feature engineering, model selection, validation, calibration, reporting, and downstream R agent handoff."

policy:
  allow_implicit_invocation: true
